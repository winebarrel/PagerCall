import SwiftUI

struct SettingView: View {
    var body: some View {
        Form {
            // TODO:
            Button {} label: {
                Image(systemName: "leaf")
            }
        }
    }
}

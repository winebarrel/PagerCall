disabled_rules:
  - line_length
  - nesting
# Require trailing commas to match SwiftFormat's trailingCommas rule.
trailing_comma:
  mandatory_comma: true
analyzer_rules:
  - unused_declaration
  - unused_import
